<!-- HTML Start -->
<header class="header-hld">
  <div class="container">
    <div class="row">
        {{page.header}}
    </div>
  </div>
</header>
{% if not user.hasPermission('administer nodes') %}
<section class="al-header">
  {{page.primary_menu}}
</section>
{% endif %}
{% if not user.hasPermission('administer nodes') %}
<section class="leftmenu-hld">
  {{page.sidebar_first}}
</section>
{% endif %}
<section class="banner-hld">
    {{page.banner}}
</section>

<div class="main-content">
<section class="content-hld pt-5 pb-2 pb-md-5">
  {{page.content}}
</section>

<footer class="footer-hld">
  {{page.footer}}
</footer>
</div>
